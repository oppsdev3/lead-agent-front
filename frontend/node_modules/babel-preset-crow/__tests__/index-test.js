const crowPreset = require('../')

describe('babel-preset-env', () => {
  it('should export a function returning babel-preset config', () => {
    expect(typeof crowPreset()).toEqual('object')
  })
})
