const { spawnPromise, parseOptions } = require('../helpers')

module.exports = ({ jestOptions, extendJestOptions }) => {
  const params = parseOptions({
    overwriteOptions: jestOptions,
    extendOptions: extendJestOptions,
  })

  return spawnPromise({ bin: 'jest', params })
}
