const { spawnPromise, parseOptions } = require('../helpers')

module.exports = ({ infoOptions, extendInfoOptions }) => {
  const params = parseOptions({
    overwriteOptions: infoOptions,
    extendOptions: extendInfoOptions
  })

  return spawnPromise({ bin: 'npm-scripts-info', params })
}
