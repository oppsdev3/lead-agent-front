const executeRunners = (promises = []) => Promise
  .all(
    promises.map(p => p.catch(e => e)))
  .then(codes =>
    process.exit(codes.reduce((a, b) => a + b, 0))
  )

module.exports = executeRunners
